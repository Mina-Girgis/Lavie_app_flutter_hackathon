import 'package:flutter/material.dart';

const Color GREEN = Color.fromRGBO(26, 188, 0, 1.0);
const Color DARK_GRAY = Color.fromRGBO(111, 111, 111, 1.0);
const Color GRAY = Color.fromRGBO(138, 138, 138, 1.0);
const Color LIGHT_GRAY = Color.fromRGBO(151, 151, 151, 1.0);

const Color SEARCH_BAR_COLOR = Color.fromRGBO(248, 248, 248, 1);

const Color BACK_GROUND_COLOR = Color.fromRGBO(253, 253, 254, 1.0);

const Color SOLID = Color.fromRGBO(0, 0, 0, 0.6);

